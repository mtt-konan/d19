# 忽略本目录所有 PDF，但保留 README.md、.gitignore 和抽取文本
*.pdf
*.PDF

# 不忽略说明文件
!README.md
!.gitignore
!*.txt

# 注：PDF 是二进制，不适合直接 review。
# 需要文本时运行：
#   uv run python scripts/extract_pdf_text.py docs/literature/pdfs/<paper>.pdf
# 生成的 .txt 可以 grep 和 review；需要时可提交作为阅读缓存。
